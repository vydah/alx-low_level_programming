#include "main.h"
/**
 * print_last_digit - Gives the last digit of a of i.
 * @i: the number to be checked.
 * Return: n
 */
int print_last_digit(int i)
{
	if (i < 0)
		i = i * -1;
	_putchar((n % 10) + '0');
	return (n % 10);
}
